# SESSION RESUME: 2026-01-07 08:44

## 🎯 Overall Plan
The current phase of the BACON-AI project (Release 2.0) focuses on commercial-grade visual controls, security hardening, and reliability. Recent work focused on the visual dashboard (BACON Dashboard) and physics engine refinement.

## ✅ Progress So Far
1.  **Physics Nomenclature**: Renamed technical D3 parameters to user-friendly terms:
    *   `Repulsion` -> **Push**
    *   `Link Distance` -> **Reach**
    *   `Tension` -> **Stiffness**
2.  **Live Physics Reheat**: Implemented `onPointerDown`/`onPointerUp` handlers on sliders to set `alphaTarget(0.3)`, ensuring the mesh responds dynamically while dragging.
3.  **Individual Node Controls**: Added "Node Size" multiplier and a **"Reset to Defaults"** button to the right-click context menu.
4.  **Cluster Separation**: Increased the default bridge link distance to `180px` to maintain visual genealogies.
5.  **Environment Compatibility**:
    *   Added a robust `crypto.randomUUID` polyfill for non-HTTPS (Hostinger) environments.
    *   Added defensive fallbacks for `d3AlphaTarget` access on the graph component.
6.  **Self-Annealing**: Recorded 5 critical lessons learned into the **Mem0 Cloud Context** and created a formal [Self-Annealing Report](documentation/technical/self_annealing_report.md).

## 🛑 Issues Faced & Resolved
*   **JSX Corruption**: Large edits in `App.tsx` caused closing tags to break. **Fixed** by manual realignment and incremental edits.
*   **Blank Screen on Deployment**: Caused by `randomUUID` returning undefined on HTTP. **Fixed** via header polyfill.
*   **D3 Ref Failures**: Occurred during browser subagent verification. **Fixed** by accessing the simulation object directly as a fallback.

## 📝 To-Dos & Next Steps
- [ ] **Release 2.1: Security Hardening**:
    *   Implement MQTT Authentication (Username/Password) in `mqtt_handler.py`.
    *   Enable TLS certificates (Port 8883) for the Control Plane.
- [ ] **Persistence**:
    *   Deploy `systemd` unit files for the persistent agent listeners.
    *   Create a cleanup script for stale SQLite entries in `bacon.db`.
- [ ] **GUI**:
    *   Add a "Global Reheat" toggle for continuous mesh motion.
    *   Implement "Mesh Heatmap" mode based on message frequency.

## 🧠 Session Context for Next Agent
- **Mem0 user_id**: Use `antigravity-agent` to recall the recent self-annealing lessons.
- **Server URL**: `http://srv906866.hstgr.cloud:8000/` (HTTP only, use polyfills).
- **Core File**: `src/dashboard/src/App.tsx` is the primary dashboard logic (>1100 lines). Be aggressive with chunking edits to avoid corruption.
- **Protocol**: V1.2 (Signal/Presence based).

---
*Generated by Antigravity @ 2026-01-07*
